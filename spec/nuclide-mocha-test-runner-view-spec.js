'use babel';

import NuclideMochaTestRunnerView from '../lib/nuclide-mocha-test-runner-view';

describe('NuclideMochaTestRunnerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
